# Схема подключения JXCT Soil Sensor v2.4.3

## 🔌 Общая схема подключения

```
    ┌─────────────────┐         ┌─────────────────┐         ┌───────────────────┐
    │     ESP32       │         │     MAX485      │         │   JXCT 7-in-1     │
    │    DevKit       │         │     Module      │         │   Soil Sensor     │
    └─────┬───────────┘         └─────┬───────────┘         └─────┬─────────────┘
          │                           │                           │
          │    ┌─────────────────────────────────────────────────────┐
          │    │                                                     │
          │    │            12-24V DC Power Supply                   │
          │    │                                                     │
          │    └─────────────────────────────────────────────────────┘
          │
    ┌─────▼──────────────────────────────────────────────────────────────┐
    │                        Подключения                                │
    └────────────────────────────────────────────────────────────────────┘
```

## 📐 Детальная схема ESP32 ↔ MAX485

```
ESP32 DevKit                    MAX485 Module
┌─────────────────┐            ┌─────────────────┐
│                 │            │                 │
│  GPIO16 (RX2) ● ├────────────┤ ● RO            │
│  GPIO17 (TX2) ● ├────────────┤ ● DI            │
│  GPIO4        ● ├────────────┤ ● DE            │
│  GPIO5        ● ├────────────┤ ● RE            │
│               │            │                 │
│  3.3V         ● ├────────────┤ ● VCC           │
│  GND          ● ├────────────┤ ● GND           │
│                 │            │                 │
│               │            │  A+ ●────────────┐
│               │            │  B- ●─────────┐  │
└─────────────────┘            └─────────────┼──┼──┘
                                              │  │
                           Витая пара RS485  │  │
                        (экранированный кабель)
                                              │  │
                                              │  │
┌─────────────────────────────────────────────┼──┼─────────┐
│                JXCT Sensor                  │  │         │
│                                             │  │         │
│  A+ (Желтый)  ●─────────────────────────────┘  │         │
│  B- (Синий)   ●────────────────────────────────┘         │
│                                                          │
│  12-24V (Красный) ●──────────── 12-24V DC Блок питания  │
│  GND (Черный)     ●──────────── GND                      │
└──────────────────────────────────────────────────────────┘
```

## 🔧 Таблица подключений

### ESP32 → MAX485
| ESP32 GPIO | MAX485 Pin | Тип сигнала | Описание |
|------------|------------|-------------|----------|
| GPIO16     | RO         | Цифровой вход | UART2 RX - прием данных от MAX485 |
| GPIO17     | DI         | Цифровой выход | UART2 TX - передача данных в MAX485 |
| GPIO4      | DE         | Цифровой выход | Driver Enable - управление передачей |
| GPIO5      | RE         | Цифровой выход | Receiver Enable - управление приемом |
| 3.3V       | VCC        | Питание | Питание модуля MAX485 |
| GND        | GND        | Общий | Общий провод |

### MAX485 → JXCT Sensor
| MAX485 | JXCT Провод | Цвет | Назначение |
|--------|-------------|------|------------|
| A+     | A+          | Желтый | RS485 Data+ |
| B-     | B-          | Синий | RS485 Data- |

### Питание JXCT Sensor (отдельно!)
| Источник | JXCT Провод | Цвет | Назначение |
|----------|-------------|------|------------|
| 12-24V DC+ | VCC | Красный | Питание датчика |
| GND | GND | Черный | Общий минус |

## ⚠️ Важные примечания

### 🔋 Питание
- **ESP32:** питается от USB или внешнего 5V
- **MAX485:** питается от 3.3V ESP32
- **JXCT Sensor:** требует отдельное питание 12-24V DC!

### 📡 RS485 линия
- Используйте **экранированный кабель** витая пара
- Максимальная длина кабеля: **1200 метров**
- При длинных линиях добавьте терминальные резисторы 120 Ом
- Подключайте экран кабеля к GND на одном конце

### 🔧 GPIO конфигурация
```cpp
// Определения в src/modbus_sensor.cpp
#define RX_PIN 16    // UART2 RX
#define TX_PIN 17    // UART2 TX  
#define DE_PIN 4     // Driver Enable
#define RE_PIN 5     // Receiver Enable
```

## 🌐 Логические уровни MAX485

### Режим передачи (TX)
```
DE = HIGH (3.3V)
RE = HIGH (3.3V)
ESP32 TX → MAX485 DI → RS485 A+/B-
```

### Режим приема (RX)  
```
DE = LOW (0V)
RE = LOW (0V)
RS485 A+/B- → MAX485 RO → ESP32 RX
```

## 🔍 Тестирование подключения

### 1. Проверка питания
```cpp
void testPowerConnections() {
    // Проверяем пины MAX485
    logSystem("DE_PIN (%d): %d", DE_PIN, digitalRead(DE_PIN));
    logSystem("RE_PIN (%d): %d", RE_PIN, digitalRead(RE_PIN));
    
    // Проверяем UART2
    logSystem("Serial2 available: %d", Serial2.available());
}
```

### 2. Проверка RS485
```cpp
void testRS485Connection() {
    // Переключение в режим передачи
    digitalWrite(DE_PIN, HIGH);
    digitalWrite(RE_PIN, HIGH);
    
    // Отправка тестового байта
    Serial2.write(0x55);
    Serial2.flush();
    
    // Переключение в режим приема
    digitalWrite(DE_PIN, LOW);
    digitalWrite(RE_PIN, LOW);
    delay(100);
    
    // Проверка ответа
    if (Serial2.available()) {
        logSuccess("RS485 работает!");
    }
}
```

### 3. Проверка MODBUS
```cpp
bool testModbusConnection() {
    // Попытка чтения версии прошивки датчика
    uint8_t result = modbus.readHoldingRegisters(0x0007, 1);
    
    if (result == modbus.ku8MBSuccess) {
        uint16_t version = modbus.getResponseBuffer(0);
        logSuccess("JXCT датчик найден! Версия: %d.%d", 
                  (version >> 8) & 0xFF, version & 0xFF);
        return true;
    } else {
        logError("Нет связи с датчиком: %d", result);
        return false;
    }
}
```

## 🐞 Типичные проблемы подключения

### 1. Нет связи с датчиком
**Симптомы:** `ku8MBResponseTimedOut` (код 226)

**Проверьте:**
- [ ] Правильность подключения A+/B- (не перепутаны местами)
- [ ] Питание датчика 12-24V (проверьте мультиметром)  
- [ ] Целостность кабеля RS485
- [ ] Адрес датчика (по умолчанию 1)

### 2. Ошибки CRC
**Симптомы:** `ku8MBInvalidCRC` (код 227)

**Проверьте:**
- [ ] Качество кабеля RS485 (витая пара, экранированный)
- [ ] Наличие помех (рядом силовые кабели)
- [ ] Заземление системы
- [ ] Терминальные резисторы на длинных линиях

### 3. Неустойчивая связь
**Симптомы:** Периодические ошибки, пропуски данных

**Решения:**
- Увеличьте паузы между запросами
- Добавьте дополнительные попытки чтения
- Проверьте качество питания датчика
- Используйте ферритовые фильтры на кабеле

## 📏 Рекомендации по монтажу

### Кабельная система
1. **Кабель RS485:** Витая пара, экранированный, 120 Ом
2. **Питание датчика:** Отдельный силовой кабель 1.5-2.5 мм²
3. **Разделение:** RS485 и питание в разных кабелях/каналах

### Заземление
1. Подключите экран кабеля к GND на стороне ESP32
2. Не заземляйте экран на двух концах (петли заземления)
3. Обеспечьте хорошее заземление блока питания датчика

### Защита от помех
1. Избегайте прокладки рядом с силовыми кабелями
2. Используйте ферритовые кольца на кабеле RS485
3. Добавьте TVS диоды для защиты от перенапряжений

---

*Схема подключения для JXCT Soil Sensor v2.4.3* 