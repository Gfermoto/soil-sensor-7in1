# Super-Linter temporarily disabled due to git checkout issues
# Will be re-enabled after fixing the underlying git problems

name: Super-Linter

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  super-linter:
    runs-on: ubuntu-latest
    steps:
      - name: Super-Linter Disabled
        run: echo "Super-Linter temporarily disabled - focusing on CodeRabbit integration" 