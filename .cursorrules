# Cursor AI Rules for JXCT Soil Sensor Project

## 🏗️ АРХИТЕКТУРА ПРОЕКТА
Это профессиональная IoT система мониторинга почвы на базе ESP32.
- **Framework:** Arduino (ESP32)
- **Язык:** C++17
- **Сборка:** PlatformIO
- **Коммуникация:** Modbus RTU, MQTT, HTTP
- **Веб-интерфейс:** Встроенный WebServer
- **OTA:** Автоматические обновления

## 🛠️ НАСТРОЕННЫЕ ИНСТРУМЕНТЫ (ВСЕГДА ИСПОЛЬЗОВАТЬ!)

### 1. СТАТИЧЕСКИЙ АНАЛИЗ
```bash
# Clang-tidy анализ (ОБЯЗАТЕЛЬНО перед изменениями)
python scripts/run_clang_tidy_analysis.py

# Cppcheck анализ
pio check -e static-analysis
```

### 2. ТЕСТИРОВАНИЕ
```bash
# Основные тесты
python -m pytest test/test_validation.py test/test_compensation_formulas.py

# Полный набор тестов
python scripts/run_simple_tests.py

# Native тесты (работают на Windows с w64devkit)
pio test -e native

# ESP32 тесты (требуют доработки - системная проблема PlatformIO)
pio test -e esp32dev-test
```

### 3. СБОРКА
```bash
# Основная сборка
pio run -e esp32dev

# Production сборка
pio run -e esp32dev-production
```

### 4. ВЕРСИОНИРОВАНИЕ
```bash
# Автоматическое обновление версии
python scripts/auto_version.py

# Синхронизация версий
python scripts/sync_versions.py
```

### 5. CODE RABBIT (АНАЛИЗ КОДА)
- Конфигурация: .coderabbit.yaml
- Безопасный режим: только анализ, без изменений
- Автоматический анализ PR

## 📁 СТРУКТУРА ПРОЕКТА
```
src/
├── main.cpp                 # Главный файл
├── config.cpp              # Конфигурация
├── modbus_sensor.cpp       # Modbus датчики
├── mqtt_client.cpp         # MQTT клиент
├── wifi_manager.cpp        # WiFi управление
├── web/                    # Веб-интерфейс
├── business/               # Бизнес-логика
└── validation_utils.cpp    # Валидация данных

test/
├── test_validation.py      # Тесты валидации ✅ РАБОТАЮТ
├── test_compensation_formulas.py  # Тесты формул ✅ РАБОТАЮТ
├── test_simple_native.cpp  # C++ тесты ✅ РАБОТАЮТ
└── esp32/
    ├── test_simple.cpp     # ESP32 тесты ⚠️ ТРЕБУЮТ ДОРАБОТКИ
    └── test_stubs.cpp      # Заглушки для тестов ✅ ДОБАВЛЕНЫ

scripts/
├── run_clang_tidy_analysis.py  # Clang-tidy ✅ РАБОТАЕТ
├── run_simple_tests.py         # Python тесты ✅ РАБОТАЮТ
└── auto_version.py             # Версионирование ✅ РАБОТАЕТ
```

## 🔧 КОМПИЛЯТОР И СРЕДА
- **Компилятор:** w64devkit (C:\Program Files\w64devkit\bin) ✅ НАСТРОЕН
- **PATH:** Добавлен в переменные среды ✅ НАСТРОЕН
- **GCC:** 15.1.0 доступен ✅ РАБОТАЕТ
- **PlatformIO:** Настроен для ESP32 ✅ НАСТРОЕН

## ⚠️ КРИТИЧЕСКИЕ ПРАВИЛА

### 1. ПЕРЕД ЛЮБЫМИ ИЗМЕНЕНИЯМИ:
```bash
# 1. Запустить статический анализ
python scripts/run_clang_tidy_analysis.py

# 2. Запустить тесты
python -m pytest test/test_validation.py

# 3. Проверить сборку
pio run -e esp32dev
```

### 2. ПОСЛЕ ИЗМЕНЕНИЙ:
```bash
# 1. Обновить версию
python scripts/auto_version.py

# 2. Запустить полные тесты
python scripts/run_simple_tests.py

# 3. Проверить сборку
pio run -e esp32dev
```

### 3. ПРИ СОЗДАНИИ PR:
- CodeRabbit автоматически проанализирует код
- CI/CD запустит все проверки
- Убедиться, что все тесты проходят

## 🎯 CODE RABBIT АНАЛИЗ (ВСЕГДА ДЕЛАТЬ)

### При анализе кода проверять:
1. **Безопасность:** Уязвимости, переполнения буферов
2. **Производительность:** Неэффективные алгоритмы
3. **Читаемость:** Сложный код, плохие имена
4. **Совместимость:** Проблемы с ESP32
5. **Стиль:** Соответствие C++17
6. **Логика:** Потенциальные баги

### Пример анализа:
```cpp
// Код пользователя
void setSensorRange(float min, float max) {
    if (min > max) return false;
    // ...
}

// Мой анализ как CodeRabbit:
🔍 АНАЛИЗ КОДА:
⚠️ ПРОБЛЕМЫ:
1. Возвращаемый тип void, но return false
2. Параметры min/max легко перепутать
3. Нет валидации входных данных

✅ РЕКОМЕНДАЦИИ:
1. Изменить тип на bool
2. Использовать именованные параметры
3. Добавить проверки на NaN/inf
```

## 📊 ТЕКУЩИЙ СТАТУС ПРОЕКТА (ОБНОВЛЕНО)

### ✅ РАБОТАЕТ ОТЛИЧНО:
- **Сборка:** `pio run -e esp32dev` ✅
- **Основные тесты:** `python scripts/run_simple_tests.py` ✅
- **Python тесты:** `python -m pytest test/` ✅
- **Native тесты:** `pio test -e native` ✅ (с w64devkit)
- **Clang-tidy:** `python scripts/run_clang_tidy_analysis.py` ✅ (125 предупреждений)
- **CodeRabbit:** Настроен ✅
- **Автоматическое версионирование:** `python scripts/auto_version.py` ✅
- **Pre-commit хуки:** Работают ✅

### ⚠️ ТРЕБУЕТ ВНИМАНИЯ:
- **ESP32 тесты:** `pio test -e esp32dev-test` ⚠️ Системная проблема PlatformIO с Arduino framework
- **Статический анализ cppcheck:** `pio check -e static-analysis` ⚠️ Требует доработки конфигурации

### 🔧 ИСПРАВЛЕНИЯ, КОТОРЫЕ МЫ СДЕЛАЛИ:
1. **Добавили w64devkit в PATH** ✅ - Native тесты теперь работают
2. **Создали ESP32 тесты** ✅ - test/esp32/test_simple.cpp
3. **Добавили заглушки** ✅ - test/esp32/test_stubs.cpp
4. **Обновили конфигурацию** ✅ - platformio.ini с правильными флагами
5. **Создали точку отката** ✅ - git tag test-fix-point
6. **Добавили QUICK_COMMANDS.md** ✅ - Быстрые команды для работы

## 🚨 ЧТО НЕ ДЕЛАТЬ
- Не изменять код без предварительного анализа
- Не игнорировать предупреждения clang-tidy
- Не коммитить код без тестирования
- Не забывать обновлять версию
- НЕ ИЗМЕНЯТЬ ОСНОВНОЙ КОД (main.cpp) без крайней необходимости

## 💡 ЛУЧШИЕ ПРАКТИКИ
- Всегда запускать тесты перед изменениями
- Использовать статический анализ
- Следовать C++17 стандартам
- Документировать сложную логику
- Проверять совместимость с ESP32
- Создавать точки отката перед крупными изменениями

## 🔄 РАБОЧИЙ ПРОЦЕСС
1. **Анализ:** clang-tidy + тесты
2. **Разработка:** Изменения кода
3. **Тестирование:** Все тесты
4. **Сборка:** Проверка компиляции
5. **Версионирование:** Обновление версии
6. **CodeRabbit:** Анализ PR
7. **Мерж:** Только после всех проверок

## 🛡️ БЕЗОПАСНОСТЬ
- **Точка отката:** git tag test-fix-point
- **Основной код:** НЕ изменён
- **Все изменения:** В отдельных тестовых файлах
- **Конфигурация:** Обновлена безопасно 